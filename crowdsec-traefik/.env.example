# Domain configuration - Replace with your actual domain
DOMAIN=your-domain.com

# Directory structure
TRAEFIK_DATA_FOLDER=data
CROWDSEC_CONFIG_FOLDER=crowdsec_config

# Basic auth credentials for Traefik dashboard (format: username:hashedpassword)
# Generate with: echo $(htpasswd -nb username password) | sed -e s/\\$/\\$\\$/g
TRAEFIK_DASHBOARD_CREDENTIALS=admin:$$2y$$05$$REPLACE_WITH_YOUR_HASHED_PASSWORD

# CrowdSec configuration
# The API key needs to be created in the crowdsec container with:
# docker compose exec crowdsec cscli bouncers add bouncer-traefik
CROWDSEC_BOUNCER_API_KEY=replace_with_your_crowdsec_bouncer_api_key

# User ID for file permissions
GID=1000

# Cloudflare API token for DNS management (ensure this is securely stored)
CF_DNS_API_TOKEN=replace_with_your_cloudflare_api_token